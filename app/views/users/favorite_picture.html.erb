<h1><%= @user.name %>のお気に入り一覧</h1>
<div class="user_icon">
  <%= image_tag "#{@user.image}"%>
</div>

<table>
  <tbody>
    <% @favorites.each do |feed| %>
      <tr>
        <td><%= feed.user.name %></td>
        <td><%= image_tag "#{feed.image}", :size =>'200x200' %></td>
        <td><%= feed.content %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to '投稿一覧へ', feeds_path %>
