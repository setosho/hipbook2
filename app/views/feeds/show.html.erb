<p>
  <strong>画像の詳細</strong>
  <br>
  <%= image_tag @feed.image.url, :size =>'200x200' %>
  <br>
  <%= @feed.content %>
</p>

<% unless @feed.user_id == current_user.id %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete, class: 'btn btn-danger' %>
  <% else %>
    <%= link_to 'お気に入りする', favorites_path(feed_id: @feed.id), method: :post, class: 'btn btn-primary' %>
  <% end %>
<% end %>

<%if @feed.user_id == current_user.id %>
  <%= link_to '画像を編集', edit_feed_path(@feed) %>
  <%= link_to '画像を削除', @feed, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>

<%= link_to '画像一覧へ戻る', feeds_path %>
